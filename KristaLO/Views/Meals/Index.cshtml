

@*@model List<KristaLO.Models.Meals>;*@

<p>show all Meals here</p>

@if (Model.Count == 0)
{
    <p>No Meals yet!</p>
}
else
{
    <table class="table">
        <tr>
            <th>Position Name</th>
            <th>Employer</th>
        </tr>
        @foreach (var meal in Model)
        {
            <tr>
                <td><a asp-controller="Home" asp-action="Detail" asp-route-id="@job.Id">@job.Name</a></td>
                <td>@job.Employer.Name</td>
            </tr>
        }
    </table>
}